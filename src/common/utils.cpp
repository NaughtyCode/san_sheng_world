#include "utils.hpp"
// All utils functions are inline in the header. This file exists
// only because the build system requires a .cpp for the static library.
namespace agent::utils {
// Placeholder to satisfy linker
} // namespace agent::utils
